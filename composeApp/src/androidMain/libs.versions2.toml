toml
[libraries]
# ... existing libraries
# Add this line:
compose-material-icons-extended = { module = "org.jetbrains.compose.components:components-ui-tooling-preview", version.ref = "composeMultiplatform" }
# However, for Multiplatform, it is usually bundled or requires the following explicit dependency:
androidx-compose-material-icons-extended = { module = "org.jetbrains.compose.material:material-icons-extended", version.ref = "composeMultiplatform" }